import { CustomdirectoryDirective } from './customdirectory.directive';

describe('CustomdirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
